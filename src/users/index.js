const {
  register,
  login,
  verifyCode,
  forgotPassword,
  resetPassword,
  sendNewCode,
} = require("./api/controller");

module.exports = {
  register,
  login,
  verifyCode,
  forgotPassword,
  resetPassword,
  sendNewCode,
};
