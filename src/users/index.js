const {
  register,
  login,
  verifyCode,
  forgotPassword,
  resetPassword,
  sendNewCode,
  sendNewAccessToken,
} = require("./api/controller");

module.exports = {
  register,
  login,
  verifyCode,
  forgotPassword,
  resetPassword,
  sendNewCode,
  sendNewAccessToken,
};
